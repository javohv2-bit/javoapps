# Scrapy project for Canon Product Scraping

## Setup
```bash
pip3 install -r requirements.txt
```

## Run spider
```bash
python3 -m scrapy crawl powershot -o output.json
```

## Output
Results saved to `output.json` with all product images, specifications, and features.
